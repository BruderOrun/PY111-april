import os
import subprocess

def choice():
    subprocess.call(['ls', '/Users/norunov/Documents/python/politech/PY111-april/the_choice' ])
    name_book_choice = input('Скопируйте и вставьте в терминал имя книги. Для добавления в библиотеку')



g = [[1,'name','disk'],['name2','disk2']]

# with open(oldfile, 'r', encoding='utf-8') as infile:
def ds():
    for i in g:

     print(g[1])